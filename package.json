{
  "name": "react-data-fetching",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev2": "turbo dev --filter 2-vite-reactquery",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  }
}
